machine:
  python:
    version: 2.7.6

test:
  post:
    - >
      if [ -n "${RUN_NIGHTLY_BUILD}" ]; then
        ./run-functional-tests.sh ${FUNCTIONAL_TEST_TARGET};
      fi

deployment:
  deploy-master:
  commands:
    - ./deploy.sh
